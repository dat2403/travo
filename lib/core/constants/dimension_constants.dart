import 'package:flutter/material.dart';

class AppDimension {
  static const double kMediumPadding = 24.0; //bigger default padding
  static const double kBottomBarIconSize = 20.0;
  static const double kDefaultPadding = 16.0;
  static const double kItemPadding = 10.0;
  static const double kMinPadding = 5.0; //minimum default padding
  static const double kTopPadding = 8.0;  //default top padding
  static const double kDefaultIconSize = 18.0;
  static const BorderRadius kDefaultBorderRadius = BorderRadius.all(Radius.circular(8));
}